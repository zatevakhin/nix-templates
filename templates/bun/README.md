# Bun

This template provides a minimal `flake-parts` development shell for Bun.

## Getting started

1. `direnv allow` (or run `nix develop` directly)
2. `bun --version` to confirm
3. `bun init` or `bun add <package>` to start
